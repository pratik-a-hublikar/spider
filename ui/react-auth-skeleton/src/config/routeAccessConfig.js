// Protected routes listed here are available to every authenticated user,
// even when they are not returned by the accessible-menu API.
export const authenticatedUserRoutes = Object.freeze([
  '/home',
]);
